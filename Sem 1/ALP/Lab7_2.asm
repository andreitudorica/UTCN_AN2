DATA SEGMENT PARA PUBLIC 'DATA'
	let		DB	'a'
DATA ENDS

CODE SEGMENT PARA PUBLIC 'CODE'
ASSUME CS:CODE, DS:DATA
START PROC FAR
	PUSH 	DS
	XOR 	AX,AX
	PUSH 	AX
	MOV 	AX,DATA
	MOV		DS,AX
	
	MOV		AL,let
	SUB		AX,20H
	MOV		let,AL
RET
START ENDP
CODE ENDS
END START	
